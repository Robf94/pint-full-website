{
  "name": "pint-full-website",
  "version": "1.0.0",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
