{
  "name": "11ty-static-site",
  "version": "1.0.0",
  "scripts": {
    "start": "@11ty/eleventy --serve",
    "build": "@11ty/eleventy",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
